<form method="post" action="{{ path('pago_administracion_delete', {'id': pago_administracion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pago_administracion.id) }}">
    <button class="btn">Delete</button>
</form>
