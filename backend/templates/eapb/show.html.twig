{% extends 'base.html.twig' %}

{% block title %}Eapb{% endblock %}

{% block body %}
    <h1>Eapb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eapb.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ eapb.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ eapb.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eapb_index') }}">back to list</a>

    <a href="{{ path('eapb_edit', {'id': eapb.id}) }}">edit</a>

    {{ include('eapb/_delete_form.html.twig') }}
{% endblock %}
