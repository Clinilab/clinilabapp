<form method="post" action="{{ path('estado_estudio_delete', {'id': estado_estudio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estado_estudio.id) }}">
    <button class="btn">Delete</button>
</form>
