{% extends 'base.html.twig' %}

{% block title %}Participante{% endblock %}

{% block body %}
    <h1>Participante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participante.id }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ participante.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participante_index') }}">back to list</a>

    <a href="{{ path('participante_edit', {'id': participante.id}) }}">edit</a>

    {{ include('participante/_delete_form.html.twig') }}
{% endblock %}
