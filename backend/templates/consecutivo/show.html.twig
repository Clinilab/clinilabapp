{% extends 'base.html.twig' %}

{% block title %}Consecutivo{% endblock %}

{% block body %}
    <h1>Consecutivo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consecutivo.id }}</td>
            </tr>
            <tr>
                <th>Prefijo</th>
                <td>{{ consecutivo.prefijo }}</td>
            </tr>
            <tr>
                <th>Consecutivo</th>
                <td>{{ consecutivo.consecutivo }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ consecutivo.Activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consecutivo_index') }}">back to list</a>

    <a href="{{ path('consecutivo_edit', {'id': consecutivo.id}) }}">edit</a>

    {{ include('consecutivo/_delete_form.html.twig') }}
{% endblock %}
