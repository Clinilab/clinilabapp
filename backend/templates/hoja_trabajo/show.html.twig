{% extends 'base.html.twig' %}

{% block title %}HojaTrabajo{% endblock %}

{% block body %}
    <h1>HojaTrabajo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hoja_trabajo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ hoja_trabajo.nombre }}</td>
            </tr>
            <tr>
                <th>Archivo</th>
                <td>{{ hoja_trabajo.archivo }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ hoja_trabajo.fecha }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ hoja_trabajo.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hoja_trabajo_index') }}">back to list</a>

    <a href="{{ path('hoja_trabajo_edit', {'id': hoja_trabajo.id}) }}">edit</a>

    {{ include('hoja_trabajo/_delete_form.html.twig') }}
{% endblock %}
