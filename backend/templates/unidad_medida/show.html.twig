{% extends 'base.html.twig' %}

{% block title %}UnidadMedida{% endblock %}

{% block body %}
    <h1>UnidadMedida</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unidad_medida.id }}</td>
            </tr>
            <tr>
                <th>Simbolo</th>
                <td>{{ unidad_medida.simbolo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ unidad_medida.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ unidad_medida.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unidad_medida_index') }}">back to list</a>

    <a href="{{ path('unidad_medida_edit', {'id': unidad_medida.id}) }}">edit</a>

    {{ include('unidad_medida/_delete_form.html.twig') }}
{% endblock %}
